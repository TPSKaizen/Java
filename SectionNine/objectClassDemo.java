package SectionNine;

public class objectClassDemo {
    // Object Class ~ java.lang.Object is the Mother of all classes

    // Main Purpose
    // 1.Acts as polymorphic type
    // 2. Includes Core Methods

    // Core methods
    // 1. toString ~ returns a string representation of the Object // Should
    // actually be overriden
    // 2. hashCode ~ returns object's hashcode(mem address in hexidecimal)
    // Used in hash tables
    // 3. equals ~ Tests object equality. Uses == operator. Usually is overriden to
    // test for logical equality(content being the same for both objects)
    // 4. getClass ~ final method returns Class object(encapsulates meta info about
    // the class)
    // Class Object -> class name, superclass name, method names..
    // 5. clone ~ Protected method that returns a copy of this object
}
